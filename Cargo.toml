[package]
name = "mdterm"
version = "0.1.0"
edition = "2024"

[dependencies]
pulldown-cmark = "0.11"
crossterm = "0.28"
syntect = { version = "5", default-features = false, features = ["default-fancy"] }
